MOZ_ENABLE_WAYLAND=1
XCURSOR_SIZE=24
GTK_THEME=BL-Lithium-dark
XKB_DEFAULT_LAYOUT=jp

# HiDPI / scaling hints for clients (adjust GDK_SCALE / QT_SCALE_FACTOR together with wlr-randr scale)
# If you use --scale 2 in wlr-randr, set these to 2 (and GDK_DPI_SCALE=0.5 for fractional feel)
GDK_SCALE=1
GDK_DPI_SCALE=1
QT_SCALE_FACTOR=1
QT_ENABLE_HIGHDPI_SCALING=1

